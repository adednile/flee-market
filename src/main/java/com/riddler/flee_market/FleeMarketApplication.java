package com.riddler.flee_market;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
/**
 * Entry point for the Flee Market Spring Boot application.
 * This class launches the entire app.
 */
@SpringBootApplication
public class FleeMarketApplication {
    /**
     * Main method that runs the Spring Boot application.
     */
    public static void main(String[] args) {
        SpringApplication.run(FleeMarketApplication.class, args);
    }

}
